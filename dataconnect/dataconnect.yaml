specVersion: "v1"
serviceId: "task-manager"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "task-manager-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
